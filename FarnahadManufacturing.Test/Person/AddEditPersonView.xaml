<UserControl x:Class="FarnahadManufacturing.Test.Person.AddEditPersonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FarnahadManufacturing.Test.Person"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button Content="Show Person List" Command="{Binding Path=ShowPersonListCommand}"/>
            <Button Content="Close Person List" Command="{Binding Path=ClosePersonListCommand}"/>
        </StackPanel>
        <TextBlock Text="Add / Edit Person View" Grid.Row="1"/>
    </Grid>
</UserControl>
